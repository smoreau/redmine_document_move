<% if User.current.allowed_to?(:move_documents, @project) %>
<%= link_to sprite_icon('move', l(:button_move)), move_document_path(@document), :class => 'icon icon-move', :accesskey => accesskey(:move) %>
<% end %>
